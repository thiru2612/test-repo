name: Manual Codex Review

on:
  pull_request:
    types:
      - labeled # - triggered by label being added (works for all branch)

permissions:
  issues: write
  pull-requests: write

jobs:
  codex-reviewed-validation:
    name: codex reviewed status validation
    runs-on: ubuntu-latest
    if: github.event.label.name == 'good first issue'
    steps:
      - name: Verify sender
        uses: actions/github-script@v7
        with:
          script: |
            # if (context.payload.label.name !== "good first issue") {
            #   core.setFailed("Label is not 'good first issue'");
            #   return;
            # }

            if (context.payload.sender.login !== "thiru") {
              core.setFailed("Sender is not thiru");
              return;
            }
